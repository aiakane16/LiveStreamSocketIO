# The Docker image to run your workspace in. Defaults to gitpod/workspace-full
image: 
  file: gitpod.Dockerfile
# Command to start on workspace startup (optional)
tasks:
  - init: pip3 install Cython Pillow scikit-learn scikit-image tensorflow==1.3.0 django djangorestframework djang-cors-headers opencv-python
